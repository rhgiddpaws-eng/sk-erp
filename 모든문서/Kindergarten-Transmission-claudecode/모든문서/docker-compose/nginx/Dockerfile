# nginx:latest 이미지를 기반으로 합니다.
FROM nginx:latest

# vim을 설치합니다.
RUN apt-get update && apt-get install -y vim

# nginx 설정 파일을 복사하거나 추가 설정이 필요하다면 여기에 추가합니다.
# COPY ./nginx.conf /etc/nginx/nginx.conf

# 필요한 경우 추가 명령어를 여기에 작성합니다.