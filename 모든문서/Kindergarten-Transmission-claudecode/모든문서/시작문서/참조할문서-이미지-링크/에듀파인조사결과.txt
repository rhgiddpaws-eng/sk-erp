필수 구현 (은행 연동 없이 가능)
1. 시스템 관리자 기능
✅ 은행 연동 불필요
- 내부 직원 계정 CRUD
- 권한 관리 (Role-Based Access Control)
- 로그인/로그아웃
2. 고객사(유치원) 관리
✅ 은행 연동 불필요
- 유치원 정보 등록 (이름, 주소, 연락처 등)
- 에듀파인 접속 정보 암호화 저장
  * ID/PW: AES-256 암호화
  * 인증서: 안전한 저장소에 파일 보관
3. 전표 입력 및 관리
✅ 은행 연동 불필요 (수동 입력 방식)
- 수입/지출 결의서 작성 화면
- 계정 과목 선택 (드롭다운)
- 금액, 적요, 거래처 입력
- 증빙서류 첨부
4. 데이터 매핑
✅ 은행 연동 불필요
- 내부 DB 데이터 → 에듀파인 양식 변환
- 예: { date, amount, account } → 에듀파인 XML/JSON
5. 에듀파인 자동 전송
⚠️ 여기만 외부 연동 필요 (은행 아님!)
- Selenium/Playwright로 에듀파인 사이트 자동화
- 로그인 → 전표 입력 → 저장

선택 구현 (은행 연동 - 나중에 추가 가능)
6. 은행 계좌 연동 (선택사항)
🔶 이 부분만 은행 연동 필요
목적: 계좌 거래내역 자동 가져오기
방법1: 오픈뱅킹 API (권장)
방법2: 스크래핑 (복잡함)

실제 개발 단계별 가이드
Phase 1: MVP (은행 연동 없이 시작)
typescript// 1단계: 수동 입력 기반 시스템
1. 관리자가 직접 거래내역 입력
   - 날짜, 수입/지출, 금액, 계정과목, 적요
   
2. 입력된 데이터를 에듀파인 양식으로 변환

3. 자동 전송 (Selenium)

✅ 이 단계는 은행 연동 없이 완전히 구현 가능!
Phase 2: 은행 연동 추가 (선택)
방법 1: 오픈뱅킹 API (권장)
typescript// 준비물:
1. 사업자등록증
2. 금융결제원 오픈뱅킹 이용기관 신청
   - 신청: https://www.open-platform.or.kr
   - 심사 기간: 2-4주
   - 비용: 연 이용료 발생 가능

// 개발 테스트 방법:
1. 오픈뱅킹 테스트베드 신청
   - 샌드박스 환경에서 개발
   - 실제 계좌 불필요
   
2. 테스트 계좌로 API 호출 연습

// 실제 구현:
const getTransactions = async (accessToken, accountId) => {
  const response = await fetch(
    'https://openapi.open-platform.or.kr/inquiry/transaction_list',
    {
      headers: {
        'Authorization': `Bearer ${accessToken}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        bank_tran_id: generateTranId(),
        fintech_use_num: accountId,
        inquiry_type: 'A', // 전체
        from_date: '20240101',
        to_date: '20240131'
      })
    }
  );
  return response.json();
};
방법 2: 스크래핑 (비권장)
python# 각 은행별로 "빠른계좌조회" 신청 필요
# 매뉴얼에 나온 방식

from selenium import webdriver

def scrape_bank_transactions(bank_id, bank_pw):
    driver = webdriver.Chrome()
    
    # 1. 은행 사이트 로그인
    driver.get('https://bank.example.com')
    driver.find_element('input[name=id]').send_keys(bank_id)
    driver.find_element('input[name=pw]').send_keys(bank_pw)
    
    # 2. 거래내역 페이지 이동
    # 3. HTML 파싱
    # 4. 데이터 추출
    
    return transactions

# ⚠️ 문제점:
# - 은행 사이트 변경시 코드 수정 필요
# - 각 은행별로 다른 코드 작성
# - 인증서 처리 복잡

실전 테스트 방법
1. 로컬 개발 환경 (지금 바로 가능)
bash# 준비물: 없음

# 테스트 시나리오:
1. Mock 데이터로 거래내역 입력
   {
     date: '2024-02-21',
     type: '수입',
     amount: 100000,
     account: '211-수업료',
     description: '3월 수업료'
   }

2. 데이터 변환 테스트
   내부 포맷 → 에듀파인 XML

3. UI/UX 테스트
   - 전표 입력 화면
   - 승인 워크플로우
   - 검색/필터링
2. 에듀파인 연동 테스트
bash# 준비물:
- 테스트용 에듀파인 계정 (교육청에 문의)
- 또는 데모 사이트 확인

# Selenium 자동화 테스트:
// edufine-bot.ts
import { chromium } from 'playwright';

async function loginEdufine(id: string, pw: string) {
  const browser = await chromium.launch({ headless: false });
  const page = await browser.newPage();
  
  await page.goto('https://edufine.go.kr');
  await page.fill('#userId', id);
  await page.fill('#userPw', pw);
  await page.click('button[type=submit]');
  
  // 인증서 로그인 처리...
}
3. 은행 연동 테스트 (선택)
bash# 방법 A: 오픈뱅킹 샌드박스
1. 금융결제원 개발자센터 회원가입
2. 테스트 앱 등록
3. 샌드박스 API 키 발급
4. Postman으로 API 호출 테스트

# 방법 B: 본인 계좌로 테스트
1. 본인 은행 계좌로 "빠른계좌조회" 신청
2. 스크래핑 코드 작성
3. 자신의 거래내역으로 테스트
```

---

## 추천 개발 순서
```
1주차: 기본 CRUD + 로그인
  - 관리자/유치원 관리
  - 전표 수동 입력

2주차: 에듀파인 자동화
  - Selenium/Playwright 연동
  - 자동 전송 기능

3주차: 핵심 기능 완성
  - 데이터 매핑
  - 검증 로직
  - 전송 결과 확인

--- 여기까지가 MVP ---

4주차 이후: 은행 연동 (선택)
  - 오픈뱅킹 API 신청
  - 계좌 자동 조회 구현

결론
지금 당장 은행 연동 없이 시작하세요!

✅ 수동 입력 기반으로 전체 시스템 구현 가능
✅ 에듀파인 자동 전송이 핵심 (은행 아님)
✅ 은행 연동은 나중에 추가 기능으로 개발

실제로 많은 유치원 ERP는 여전히 수동 입력 방식으로 운영됩니다. 
은행 연동은 "있으면 좋은" 기능이지 필수가 아닙니다.

하지만 이 프로젝트는 옛날 방식을 선호하고 스크래핑,전송 기능을 사용함!!!!!