services:
  moavo_habit_db:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: moavo_habit
    restart: always
    shm_size: 128mb
    networks:
      - yesnetwork
    ports:
      - "9432:5432"
    volumes:
      - kindergarten_db_volume:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    environment:
      POSTGRES_PASSWORD: postgres # must change your own password.
      POSTGRES_USER: postgres # should change your own user name.

networks:
  yesnetwork:
    external: true

volumes:
  moavo_habit_db_volume:  
    name: moavo_habit_db_volume
